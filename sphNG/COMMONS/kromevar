c     extra variables for krome
      INTEGER usekrome, nmols
      REAL*8  mfracs
      CHARACTER*16 molnames

      COMMON /kromevar/ usekrome,nmols,mfracs(krome_nmols,idim),
     &  	      molnames(krome_nspec)